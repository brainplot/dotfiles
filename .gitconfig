[user]
	name = Gianluca Recchia
	email = gianluca.recchia@protonmail.com
	signingkey = CBB5274864FBF0DCCCB9230C14354796B08C3832
[commit]
	gpgsign = true
	verbose = true
[core]
	autocrlf = input
	pager = less -x1,5
[alias]
	ignore = !curl -sLw $'\\n' https://www.toptal.com/developers/gitignore/api/\"$(echo $@ | tr -s '[:blank:]' ,)\"
	lol = log --graph --pretty=birdseye
	pristine = !git reset --hard && git clean -fdx
	squeeze = !git reflog expire --all --expire=now && git gc --prune=now --aggressive
	psu = !git push -u "${1:-origin}" "$(git rev-parse --abbrev-ref HEAD)"
	root = rev-parse --show-toplevel
	dsclone = "!repo=\"$1\" && shift && \
		git clone \"$@\" git@github.com:desotech-it/\"$repo\".git \"$(printf \"$repo\" | awk '{ print tolower($0) }')\" && \
		git -C \"$repo\" desotech"
	desotech = !git config --local user.email g.recchia@desotech.it && git config --local commit.gpgSign false
	ic = commit -m 'Initial commit'
	update-file = !git commit -m \"Update \\`$1\\`\" -- \"$1\"
	add-file = !git add -- \"$1\" && git commit -m \"Add \\`$1\\`\" -- \"$1\"
	can = commit --amend --no-edit
	caan = commit -a --amend --no-edit
	pp = push
	pl = pull
	package = !git bundle create ../\"$(basename \"$(git rev-parse --show-toplevel)\")\".bundle --all
[url "git@github.com:"]
	insteadOf = gh:
[url "git@gitlab.com:"]
	insteadOf = gl:
[url "https://aur.archlinux.org/"]
	insteadOf = aur:
[pull]
	rebase = true
[pretty]
	birdseye = %C(auto)%h%d% s %C(bold blue)<%aN> %C(green)(%cr)%Creset
[init]
	defaultBranch = main
[tag]
	sort = -version:refname
[versionsort]
	suffix = -
